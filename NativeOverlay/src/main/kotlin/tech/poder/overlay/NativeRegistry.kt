package tech.poder.overlay

import java.lang.invoke.MethodHandle

object NativeRegistry {
    val registery = mutableListOf<MethodHandle>()
    fun register(discription: FunctionDescription): Int {
        TODO()
    }
}